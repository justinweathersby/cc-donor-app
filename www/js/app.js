
var app = angular.module('cc-donor-app', ['ionic',
                                          'ngResource',
                                          'ion-autocomplete'])
  .run(function($ionicPlatform) {
    $ionicPlatform.ready(function() {
            // Stuff in here
    });
});
